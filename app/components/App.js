import React from 'react';
import SongStructure from '../containers/SongStructure';
import AddElement from '../containers/AddElement';

const App = () => (
  <div>
    <AddElement />
    <SongStructure />
  </div>
);

export default App;
